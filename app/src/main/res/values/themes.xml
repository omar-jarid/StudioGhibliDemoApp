<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.StudioGhibliDemoApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/transparent</item>
    </style>

    <!-- Splash Screen per Android fino a 12 (API <= 30). -->
    <style name="Theme.SplashScreenApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/dark_gray</item>

    </style>

    <style name="Theme.SplashScreenTheme" parent="Theme.SplashScreenApp">
        <item name="android:windowBackground">@drawable/ic_studio_ghibli_splash</item>
        <item name="android:duration">1000</item>
    </style>
</resources>